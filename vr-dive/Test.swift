import CompositorServices
import Spatial

func test(drawable: LayerRenderer.Drawable) {
  let view = drawable.views[0]
  // _ = view.projection
  // _ = view.computeProjection(near: 0.1, far: 100.0)
}
